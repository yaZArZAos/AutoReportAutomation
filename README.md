# AutoReportAutomation
Автоматизация сбора и анализа продаж из файлов Excel и CSV. Скрипт объединяет все файлы из папки input, строит график суммарных продаж по датам и сохраняет объединённый отчёт в формате Excel.

📂 Структура проекта
AutoReportAutomation/
│
├─ auto_report.py        # Основной скрипт
├─ input/                # Папка для исходных файлов CSV и Excel
└─ output/               # Папка для готовых отчетов и графиков



input/ — сюда помещаем все исходные файлы .csv и .xlsx.

output/ — скрипт автоматически создаёт эту папку и сохраняет туда:

combined_data.xlsx — объединённые данные

sales_chart.png — график суммарных продаж

⚡ Как использовать:

Убедись, что у тебя установлен Python 3.12 (или выше) и необходимые библиотеки:

pip install pandas matplotlib openpyxl

Скопируй свои файлы Excel/CSV в папку input.

Запусти скрипт:
    python auto_report.py
    В папке output появятся:
        объединённый файл combined_data.xlsx
        график продаж sales_chart.png

📝 Примечания:
    Скрипт автоматически объединяет все файлы в одну таблицу и добавляет колонку source, чтобы видеть, из какого файла пришли данные.
    График строится по сумме продаж за каждый день.

📊 Пример данных:

input/sales_january.xlsx

date	sales
2026-01-01	120
2026-01-02	180

output/combined_data.xlsx

date	sales	source
2026-01-01 00:00:00 120	sales_january.xlsx
2026-01-02 00:00:00	180	sales_january.xlsx

⚙️ Требования:
    Python 3.12+
    pandas
    matplotlib

openpyxl (для работы с Excel)

pip install pandas matplotlib openpyxl
